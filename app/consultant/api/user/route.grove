route {
  POST update_user {
    data result {
      invoke user.update_user {
        first_name: input.body.first_name,
        last_name: input.body.last_name,
        phone: input.body.phone,
        email: input.body.email
      }
    }
  }
}
